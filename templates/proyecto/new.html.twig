{% extends 'base.html.twig' %}

{% block title %}Nuevo Proyecto{% endblock %}

{% block body %}
<div class="container py-4">
    <h1 class="mb-4">Crear nuevo Proyecto</h1>
    <div class="card p-4 shadow-sm">
        {{ include('proyecto/_form.html.twig') }}
    </div>
    <a href="{{ path('app_proyecto_index') }}" class="btn btn-secondary mt-3">Volver a la lista</a>
</div>
{% endblock %}
